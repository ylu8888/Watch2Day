import React from 'react';

const Heading = (props) => {
    return (
        <div className='col'>
            <h3 className='heading'>{props.heading}</h3>
        </div>
    )
}


export default Heading;